{
  "name": "@mextrack/auth",
  "version": "1.0.0",
  "description": "Shared authentication module using Lucia",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "lucia": "^3.0.1",
    "@lucia-auth/adapter-postgresql": "^3.0.0",
    "@node-rs/argon2": "^1.7.0",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0",
    "@vitest/coverage-v8": "^1.1.0"
  }
}
