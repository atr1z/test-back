# Test Environment Configuration
# This file is used specifically for E2E testing

NODE_ENV=test
PORT=3099

# Test Database (separate from development)
# Make sure to create these databases: createdb mextrack_test && createdb atriz_core_test
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mextrack_test
SHARED_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/atriz_core_test

# Database Pool Settings (smaller for tests)
DB_POOL_MAX=5
DB_SSL_ENABLED=false

# Authentication
JWT_SECRET=test-jwt-secret-key-for-testing-only-change-in-production
JWT_EXPIRES_IN=1h

# CORS
CORS_ORIGIN=http://localhost:5173

# Redis (for real-time features, if using)
REDIS_URL=redis://localhost:6379/1

# WebSocket (if using real-time tracking)
WS_CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=error

# Disable external services in tests
# Add any API keys or external service configs here with test values
