name: Mextrack API

on:
  push:
    branches: [main, develop]
    paths:
      - 'apps/mextrack/**'
      - 'packages/**'
  pull_request:
    branches: [main, develop]
    paths:
      - 'apps/mextrack/**'
      - 'packages/**'

jobs:
  build-and-test:
    name: Build and Test Mextrack
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Mextrack
        run: pnpm build:mextrack

      - name: Test Mextrack
        run: pnpm test:mextrack

      # å- name: Lint Mextrack
        # run: pnpm --filter @atriz/mextrack-api lint

  # Placeholder for deployment (to be implemented later)
  # deploy:
  #   name: Deploy Mextrack
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   
  #   steps:
  #     - name: Deploy to production
  #       run: echo "Deploy to production server"

