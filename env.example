# PostgreSQL Connection Configuration (OPTIONAL)
# These credentials are used by database management scripts to connect to PostgreSQL server
#
# NOTE: This file is OPTIONAL! The scripts can also:
# 1. Parse credentials from packages/core/.env (SHARED_DATABASE_URL)
# 2. Use default values (postgres/postgres@localhost:5432)
#
# Only create this file if you need custom PostgreSQL credentials different from your apps

# PostgreSQL Host
PGHOST=localhost

# PostgreSQL Port
PGPORT=5432

# PostgreSQL User (must have CREATE DATABASE privileges)
PGUSER=postgres

# PostgreSQL Password
PGPASSWORD=postgres

# ===== CREDENTIAL PRIORITY =====
# 1. This root .env file (if it exists)
# 2. Parsed from packages/core/.env DATABASE_URL
# 3. Default values (postgres/postgres@localhost:5432)

